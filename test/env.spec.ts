// import { Env } from '../jsxgraph/utils/env.js';


////////////////// prototype test
describe('env type suite description', () => {
    it('test description', () => {
        let a = 0
        expect(a).toBe(0)
    });
});

